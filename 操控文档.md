# BanZiXueXi 操控文档

## 1. 当前标准开发结构

```text
Yingluo2                      # 主工程
│  ├─ CMakeLists.txt
│  ├─ sdkconfig.defaults
│  ├─ partitions.csv
│  ├─ main
│  │  ├─ CMakeLists.txt
│  │  └─ main.c
│  └─ components
│     ├─ board_config
│     ├─ servo
│     ├─ led_group
│     ├─ audio_io
│     ├─ IIC                    # I2C 底层（iic.c / iic.h）
│     └─ XL9555                 # IO 扩展芯片 XL9555（xl9555.c / xl9555.h）
└─ 操控文档.md
```

## 2. 硬件组件映射（你当前需求）

本项目按以下模块拆分：

- 喇叭：`components/audio_io`
- 麦克风：`components/audio_io`
- SG90S 舵机 1/2：`components/servo`
- LED 灯组 A/B：`components/led_group`
- 板级引脚统一管理：`components/board_config`
- I2C 总线底层（GPIO/端口/频率）：`components/IIC`
- XL9555 16 路 IO 扩展（依赖 IIC）：`components/XL9555`

## 3. 你后续只需要改哪里

- 改主流程：`main/main.c`
- 改引脚定义：`components/board_config/board_config.c`
- 改舵机逻辑：`components/servo/servo.c`
- 改 LED 逻辑：`components/led_group/led_group.c`
- 改音频逻辑：`components/audio_io/audio_io.c`
- 改 I2C 引脚与速率：`components/IIC/iic.h`、`components/IIC/iic.c`
- 改 XL9555 功能脚、中断脚、器件地址：`components/XL9555/xl9555.h`、`components/XL9555/xl9555.c`

## 4. 编译与烧录（ESP32-S3）

在 `firmware` 目录执行：

```powershell
idf.py set-target esp32s3
idf.py build
idf.py -p COM3 flash monitor
```

说明：

- 如果不是 `COM3`，替换成你的实际串口（设备管理器里查看）
- 退出串口监视：`Ctrl + ]`

## 5. 当前代码状态说明

- 已可编译的主工程骨架完成
- 双舵机/双LED 已接入主循环示例
- 音频模块已预留初始化骨架（下一步接 I2S 驱动）
- 已加入正点原子风格的 **IIC**、**XL9555** 源码（便于接扩展板上的 I2C 外设与 IO 扩展）；当前目录下尚未各写 `CMakeLists.txt`，`main` 也未 `REQUIRES` 它们，因此默认构建里还不会编进这两个组件；要用时在各自目录补组件描述并在 `main/CMakeLists.txt`（或其它组件）里声明依赖即可
- 旧工程全部保留在 `examples/legacy`，不会丢失

## 6. 重要约定（我们后续沟通按这个来）

从现在开始你给我需求时，默认按以下方式执行：

1. 目标工程固定为 `firmware`
2. 你给“功能需求 + 硬件连接关系”
3. 我直接改代码/新建文件/删除文件并同步更新本操控文档

## 7. 下一步你建议直接给我的信息

为了把喇叭和麦克风真正驱动起来，请你下一条给我：

- 喇叭模块型号（如 MAX98357A）
- 麦克风模块型号（如 INMP441）
- 每个模块实际接线 GPIO
- 两个舵机和两组 LED 的最终 GPIO
- LED 是高电平亮还是低电平亮

我拿到后会直接把 `audio_io` 从骨架升级为可运行驱动，并把整套引脚配置固化到 `board_config`。
