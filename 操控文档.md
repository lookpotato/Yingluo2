# BanZiXueXi 操控文档

## 1. 当前标准开发结构

```text
Yingluo2                      # 主工程根目录
│  ├─ CMakeLists.txt
│  ├─ sdkconfig.defaults
│  ├─ partitions.csv
│  ├─ main
│  │  ├─ CMakeLists.txt
│  │  └─ main.c
│  ├─ components
│  │  ├─ board_config          # 板级引脚统一管理
│  │  ├─ servo                 # SG90S 舵机
│  │  ├─ led_group             # LED 灯组
│  │  ├─ audio_io              # ES8388 + I2S 喇叭试音
│  │  ├─ IIC                  # I2C 底层（iic.c / iic.h）
│  │  └─ XL9555               # IO 扩展芯片 XL9555（按键 / 功放使能）
│  └─ 操控文档.md
```

## 2. 当前硬件映射

| 功能 | 当前代码位置 | 关键引脚 / IO |
| --- | --- | --- |
| KEY0 / KEY1 / KEY2 | `components/XL9555` | XL9555 输入脚，主循环读取 `KEY0` / `KEY1` / `KEY2` |
| 喇叭试音 | `components/audio_io` | ES8388 + I2S，输出 440Hz 正弦波 |
| 喇叭功放使能 | `components/XL9555` | `SPK_EN_IO`，试音打开时拉高 |
| ES8388 I2C | `components/IIC` | IIC0：SDA=`GPIO41`，SCL=`GPIO42`，400kHz |
| ES8388 I2S | `components/board_config` | MCLK=`GPIO0`，BCLK=`GPIO17`，WS=`GPIO18`，DOUT=`GPIO16` |
| 舵机 1 / 3 | `components/servo` | SERVO_1=`GPIO4`，SERVO_3=`GPIO5` |
| LED 灯组 A / B | `components/led_group` | LED_A=`GPIO15`，LED_B=`GPIO6` |

如果实物原理图里的 ES8388 MCLK 不是 `GPIO0`，只需要改 `components/board_config/board_config.c` 里的 `gpio_speaker_mclk`。

## 3. KEY2 喇叭试音行为

- KEY2 未按下为 `1`，按下为 `0`。
- 每次 KEY2 按下沿切换一次试音状态。
- 串口会打印：

```text
扬声器试音 开 (audio ready=1)
扬声器试音 关 (audio ready=1)
```

`audio ready=1` 表示 I2S 任务已启动；如果为 `0`，优先看前面的 `audio_io_init failed` 或 I2S 初始化错误日志。

## 4. 编译与烧录（ESP32-S3）

在实际工程根目录 `C:\Users\16007\Desktop\BanZiXueXi\Yingluo2` 执行：

```powershell
. C:\Espressif\frameworks\esp-idf-v5.5.2\export.ps1
idf.py build
idf.py -p COM3 flash monitor
```

说明：

- 如果不是 `COM3`，替换成设备管理器里的实际串口。
- 退出串口监视：`Ctrl + ]`。
- 当前工程已经在加载 ESP-IDF 环境后验证过 `idf.py build`。

## 5. 喇叭无声排查顺序

1. 看 KEY2 日志：确认按下后出现 `扬声器试音 开`。
2. 看 `audio ready`：如果不是 `1`，先处理 I2S 初始化错误。
3. 看 ES8388 日志：启动时会先试 I2C 地址 `0x10`，失败后自动试 `0x11`；如果两个都失败，检查 I2C SDA/SCL 和 ES8388 供电。
4. 看 I2S 引脚：确认 MCLK/BCLK/WS/DOUT 分别接到 `MCLK=GPIO3
BCLK/SCK=GPIO46
WS/LRCK=GPIO9
DOUT/SDIN=GPIO10`，串口启动日志会打印这些值。
5. 看功放使能：试音打开时会打印 `SPK_EN_IO=1，功放使能`，代码会再次拉高 XL9555 的 `SPK_EN_IO`。
6. 如果日志全部正常但仍无声，优先核对实物原理图里的 ES8388 MCLK 引脚和功放使能电平是否与当前默认值一致。

## 6. 后续改哪里

- 改主流程 / 按键行为：`main/main.c`
- 改板级引脚：`components/board_config/board_config.c`
- 改舵机逻辑：`components/servo/servo.c`
- 改 LED 逻辑：`components/led_group/led_group.c`
- 改音频试音 / I2S：`components/audio_io/audio_io.c`
- 改 ES8388 初始化：`components/audio_io/es8388_hw.c`
- 改 I2C 引脚与速率：`components/IIC/iic.h`、`components/IIC/iic.c`
- 改 XL9555 功能脚、中断脚、器件地址：`components/XL9555/xl9555.h`、`components/XL9555/xl9555.c`

## 7. 当前代码状态说明

- IIC、XL9555、ES8388、I2S 试音均已接入构建。
- KEY0 控制 SERVO_3，KEY1 控制 SERVO_1。
- KEY2 控制 ES8388 喇叭试音开关。
- 本次只做 ES8388 扬声器链路，没有使用 `BEEP_IO` 蜂鸣器试音。
